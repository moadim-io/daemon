---
"moadim": patch
---

Allow manually edited `schedule.cron` sidecars to carry multiple pure cron entries, write the cron-union result to gitignored `.compailed.cron`, and sync crontab from that compiled output.
